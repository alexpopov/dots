### Internal
config/nvim/plugged
config/nvim/plugin
config/nvim/sessions
config/bash/private
config/nvim/lua/private/
config/nvim/private/
config/lazygit/state.yml

# Systemd created files
default.target.wants/
sockets.target.wants/

# Lock-file for lazy-git
config/nvim/lazy-lock.json

config/karabiner/automatic_backups/


### macOS:
# General
.DS_Store
.AppleDouble
.LSOverride

# Brew lock file, we don't want to force everyone to use the same versions
Brewfile.lock.json

# Icon must end with two \r
Icon

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

### vim:
# Swap
[._]*.s[a-v][a-z]
!*.svg  # comment out if you don't need vector files
[._]*.sw[a-p]
[._]s[a-rt-v][a-z]
[._]ss[a-gi-z]
[._]sw[a-p]

# Session
Session.vim
Sessionx.vim

# Temporary
.netrwhist
*~
# Auto-generated tag files
tags
# Persistent undo
[._]*.un~

